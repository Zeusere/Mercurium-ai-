import { Shade, Color } from './interfaces.cjs';

declare const generateColor: ({ hex, preserve, shades }: {
    hex: string;
    preserve: boolean;
    shades: Shade[];
}) => Color;

export { generateColor };
